# Build output
dist/

# Lock files (we use bun.lock)
bun.lockb
package-lock.json
pnpm-lock.yaml
yarn.lock

node_modules/

# Setuid launcher build artifact (built by `make`; only the source is tracked)
vela-browser-sandbox/vela-browser-sandbox
